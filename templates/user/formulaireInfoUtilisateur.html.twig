    <div class="justify-content-center" align="center">
        {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
        
        {# Les erreurs générales du formulaire #}
        {{ form_errors(form) }}

        {# Génération manuelle du formulaire #}

        <div class="form-group mx-16 sm:mx-16 md:mx-16 lg:mx-16 xl:mx-40 2xl:mx-60">
            {# Génération du label #}
            {{ form_label(form.username, "Nom d'utilisateur", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.username) }}
            <div class="col-sm-10">
                {# Génération de l'input. #}
                {{ form_widget(form.username, {'attr': {'class': 'form-control mb-5 p-3 w-80 focus:border-purple-700 rounded border-2 outline-none'}}) }}
            </div>
        </div>

        <div class="form-group mx-16 sm:mx-16 md:mx-16 lg:mx-16 xl:mx-40 2xl:mx-60" style="margin-top: 2rem;">
            {# Génération du label #}
            {{ form_label(form.gender, "Genre du MC", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.gender) }}
            <div class="col-sm-10">
                {# Génération de l'input. #}
                {{ form_widget(form.gender, {'attr': {'class': 'form-control mb-5 p-3 w-80 focus:border-purple-700 rounded border-2 outline-none'}}) }}
            </div>
        </div>

        <div class="form-group mx-16 sm:mx-16 md:mx-16 lg:mx-16 xl:mx-40 2xl:mx-60" style="margin-top: 2rem;">
            {# Génération du label #}
            {{ form_label(form.quartz, "Nombre de Saint Quartz", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.quartz) }}
            <div class="col-sm-10">
                {# Génération de l'input. #}
                {{ form_widget(form.quartz, {'attr': {'class': 'form-control mb-5 p-3 w-80 focus:border-purple-700 rounded border-2 outline-none'}}) }}
            </div>
        </div>
        
        <div class="form-group mx-16 sm:mx-16 md:mx-16 lg:mx-16 xl:mx-40 2xl:mx-60" style="margin-top: 2rem;">
            {# Génération du label #}
            {{ form_label(form.bronzeApple, "Nombre de Bronze Apple", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.bronzeApple) }}
            <div class="col-sm-10">
                {# Génération de l'input. #}
                {{ form_widget(form.bronzeApple, {'attr': {'class': 'form-control mb-5 p-3 w-80 focus:border-purple-700 rounded border-2 outline-none'}}) }}
            </div>
        </div>

        <div class="form-group mx-16 sm:mx-16 md:mx-16 lg:mx-16 xl:mx-40 2xl:mx-60" style="margin-top: 2rem;">
            {# Génération du label #}
            {{ form_label(form.silverApple, "Nombre de Silver Apple", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.silverApple) }}
            <div class="col-sm-10">
                {# Génération de l'input. #}
                {{ form_widget(form.silverApple, {'attr': {'class': 'form-control mb-5 p-3 w-80 focus:border-purple-700 rounded border-2 outline-none'}}) }}
            </div>
        </div>

        <div class="form-group mx-16 sm:mx-16 md:mx-16 lg:mx-16 xl:mx-40 2xl:mx-60" style="margin-top: 2rem;">
            {# Génération du label #}
            {{ form_label(form.goldenApple, "Nombre de Golden Apple", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.goldenApple) }}
            <div class="col-sm-10">
                {# Génération de l'input. #}
                {{ form_widget(form.goldenApple, {'attr': {'class': 'form-control mb-5 p-3 w-80 focus:border-purple-700 rounded border-2 outline-none'}}) }}
            </div>
        </div>

        <div class="form-group mx-16 sm:mx-16 md:mx-16 lg:mx-16 xl:mx-40 2xl:mx-60" style="margin-top: 2rem;">
            {# Génération du label #}
            {{ form_label(form.manaPrism, "Nombre de Mana Prism", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.manaPrism) }}
            <div class="col-sm-10">
                {# Génération de l'input. #}
                {{ form_widget(form.manaPrism, {'attr': {'class': 'form-control mb-5 p-3 w-80 focus:border-purple-700 rounded border-2 outline-none'}}) }}
            </div>
        </div>

        <div class="form-group mx-16 sm:mx-16 md:mx-16 lg:mx-16 xl:mx-40 2xl:mx-60" style="margin-top: 2rem;">
            {# Génération du label #}
            {{ form_label(form.playerLevel, "Niveau du joueur", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.playerLevel) }}
            <div class="col-sm-10">
                {# Génération de l'input. #}
                {{ form_widget(form.playerLevel, {'attr': {'class': 'form-control mb-5 p-3 w-80 focus:border-purple-700 rounded border-2 outline-none'}}) }}
            </div>
        </div>

        <div class="form-group mx-16 sm:mx-16 md:mx-16 lg:mx-16 xl:mx-40 2xl:mx-60" style="margin-top: 2rem;">
            {# Génération du label #}
            {{ form_label(form.gameVersion, "Version de jeu du compte", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.gameVersion) }}
            <div class="col-sm-10">
                {# Génération de l'input. #}
                {{ form_widget(form.gameVersion, {'attr': {'class': 'form-control mb-5 p-3 w-80 focus:border-purple-700 rounded border-2 outline-none'}}) }}
            </div>
        </div>
        <p class="d-flex justify-content-center">

        {# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
        <div class="mb-5">
            {{ form_widget(form.ajouter, {'attr': {'class': 'px-4 py-2 rounded-md text-sm font-medium border-b-2 focus:outline-none focus:ring transition text-white bg-green-500 border-green-800 hover:bg-green-600 active:bg-green-700 focus:ring-green-300'}}) }}
        </div>
        {# Fermeture de la balise <form> de formulaire HTML #}
        {{ form_end(form) }}
    </div>

